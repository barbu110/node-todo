/**
 * @providesModule PermissionsConstants
 */

const PermissionsConstants = {
    viewPrivateProfileData: 1,
    viewPrivateItems: 2,
    deletePosts: 4,
};

/**
 * @todo Implement this method.
 * @param object
 * @return {*}
 */
function uniqueValues(object) {
    return object;
}

export default uniqueValues(PermissionsConstants);
